<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bootdo.cpe.dao.SurverExpertAvoidanceDao">

    <select id="get" resultType="com.bootdo.cpe.domain.SurverExpertAvoidanceDO">
        SELECT * FROM ass_surver_expert_avoidance WHERE id = #{value} AND deleted = 0
    </select>

    <select id="list" resultType="com.bootdo.cpe.domain.SurverExpertAvoidanceDO">
        SELECT * FROM ass_surver_expert_avoidance
        <where>
            deleted = 0
            <if test="id != null and id != ''">AND id = #{id}</if>
            <if test="taskId != null and taskId != ''">AND task_id = #{taskId}</if>
            <if test="proId != null">AND pro_id = #{proId}</if>
            <if test="expertUserId != null">AND expert_user_id = #{expertUserId}</if>
            <if test="avoidanceType != null and avoidanceType != ''">AND avoidance_type = #{avoidanceType}</if>
        </where>
        ORDER BY created DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM ass_surver_expert_avoidance
        <where>
            deleted = 0
            <if test="id != null and id != ''">AND id = #{id}</if>
            <if test="taskId != null and taskId != ''">AND task_id = #{taskId}</if>
            <if test="proId != null">AND pro_id = #{proId}</if>
            <if test="expertUserId != null">AND expert_user_id = #{expertUserId}</if>
            <if test="avoidanceType != null and avoidanceType != ''">AND avoidance_type = #{avoidanceType}</if>
        </where>
    </select>

    <insert id="save" parameterType="com.bootdo.cpe.domain.SurverExpertAvoidanceDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ass_surver_expert_avoidance
        (task_id, pro_id, expert_user_id, avoidance_type, avoidance_reason, created_by, deleted)
        VALUES
        (#{taskId}, #{proId}, #{expertUserId}, #{avoidanceType}, #{avoidanceReason}, #{createdBy}, 0)
    </insert>

    <insert id="batchSave" parameterType="java.util.List">
        INSERT INTO ass_surver_expert_avoidance
        (task_id, pro_id, expert_user_id, avoidance_type, avoidance_reason, created_by, deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.taskId}, #{item.proId}, #{item.expertUserId}, #{item.avoidanceType}, #{item.avoidanceReason}, #{item.createdBy}, 0)
        </foreach>
    </insert>

    <update id="remove">
        UPDATE ass_surver_expert_avoidance SET deleted = 1 WHERE id = #{value}
    </update>

    <update id="batchRemove">
        UPDATE ass_surver_expert_avoidance SET deleted = 1
        WHERE id IN
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <select id="checkAvoidance" resultType="int">
        SELECT COUNT(*) FROM ass_surver_expert_avoidance
        WHERE task_id = #{taskId}
          AND pro_id = #{proId}
          AND expert_user_id = #{expertUserId}
          AND deleted = 0
    </select>

    <select id="getAvoidedProIds" resultType="java.lang.Integer">
        SELECT DISTINCT pro_id FROM ass_surver_expert_avoidance
        WHERE task_id = #{taskId}
          AND expert_user_id = #{expertUserId}
          AND deleted = 0
    </select>

    <select id="getAvoidedExpertIds" resultType="java.lang.Integer">
        SELECT DISTINCT expert_user_id FROM ass_surver_expert_avoidance
        WHERE task_id = #{taskId}
          AND pro_id = #{proId}
          AND deleted = 0
    </select>

</mapper>
