<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bootdo.common.dao.FileDao">

	<select id="get" resultType="com.bootdo.common.domain.FileDO">
		select `id`,`type`,`url`,`create_date` from sys_file where id = #{value}
	</select>

	<select id="list" resultType="com.bootdo.common.domain.FileDO">
		select `id`,`type`,`url`,`create_date` from sys_file
        <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="type != null and type != ''"> and type = #{type} </if>
		  		  <if test="url != null and url != ''"> and url = #{url} </if>
		  		  <if test="createDate != null and createDate != ''"> and create_date = #{createDate} </if>
		  		</where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<select id="listUploadEnterpriseDocs" resultType="com.bootdo.activiti.domain.EnterpriseDocUploadDo">
		SELECT
		    ad.*, sf.url
		FROM
		    ass_enterprise_docs ad
		JOIN
		    sys_file sf ON ad.file_id = sf.id
        <where>
		  		  <if test="proId != null and proId != ''"> and pro_id = #{proId} </if>
		  		  <if test="type != null and type != ''"> and sf.type = #{type} </if>
		  		  <if test="url != null and url != ''"> and sf.url = #{url} </if>
		  		  <if test="createDate != null and createDate != ''"> and sf.create_date = #{createDate} </if>
		</where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
			<otherwise>
                order by sf.id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>


	<select id="listUploadTaskDocs" resultType="com.bootdo.activiti.domain.EnterpriseDocUploadDo">
		SELECT
		ad.*
		FROM
		ass_enterprise_docs ad

		<where>
			<if test="proId != null and proId != ''"> and pro_id = #{proId} </if>
			<if test="fileType != null and fileType != ''"> and file_type = #{fileType} </if>

		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>

		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>




	<select id="listTaskDocInfo" resultType="com.bootdo.activiti.domain.EnterpriseDocUploadDo">
		SELECT
		ad.*, sf.url
		FROM
		ass_enterprise_docs ad
		JOIN
		sys_file sf ON ad.file_id = sf.id
		<where>
			<if test="proId != null and proId != ''"> and pro_id = #{proId} </if>
			<if test="taskId != null and taskId != ''"> and task_id = #{taskId} </if>
			<if test="url != null and url != ''"> and sf.url = #{url} </if>
		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
			<otherwise>
				order by sf.id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>



 	<select id="count" resultType="int">
		select count(*) from sys_file
		 <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="type != null and type != ''"> and type = #{type} </if>
		  		  <if test="url != null and url != ''"> and url = #{url} </if>
		  		  <if test="createDate != null and createDate != ''"> and create_date = #{createDate} </if>
		  		</where>
	</select>
	 
	<insert id="save" parameterType="com.bootdo.common.domain.FileDO" useGeneratedKeys="true" keyProperty="id">
		insert into sys_file
		(
			`type`, 
			`url`, 
			`create_date`
		)
		values
		(
			#{type}, 
			#{url}, 
			#{createDate}
		)
	</insert>

	<insert id="saveEnterpriseDoc" parameterType="com.bootdo.activiti.domain.EnterpriseDocUploadDo" useGeneratedKeys = "true" keyProperty = "id">
		insert into ass_enterprise_docs(pro_id,file_id,file_type) values(#{proId},#{fileId},#{fileType})
	</insert>
	 
	<update id="update" parameterType="com.bootdo.common.domain.FileDO">
		update sys_file 
		<set>
			<if test="type != null">`type` = #{type}, </if>
			<if test="url != null">`url` = #{url}, </if>
			<if test="createDate != null">`create_date` = #{createDate}</if>

			<if test="createDate != null">`create_date` = #{createDate}</if>

		</set>
		where id = #{id}
	</update>


	<update id="updateEnterpriseDoc" parameterType="com.bootdo.activiti.domain.EnterpriseDocUploadDo">
		update  ass_enterprise_docs ad
		<set>
			<if test="fileType != null">`file_type` = #{fileType}, </if>
			<if test="url != null">`url` = #{url}, </if>
			<if test="proId != null and proId != ''">  pro_id = #{proId} </if>
			<if test="taskId != null and taskId != ''">  task_id = #{taskId} </if>
		</set>
		where id = #{id}
		<if test="taskId != null and taskId != ''" >
			and task_id = ''
		</if>
	</update>

	<delete id="removeEnterpriseDoc">
		 delete from ass_enterprise_docs where  id = #{docId}
	</delete>
	<delete id="removeEnterpriseDocByFileId">
		delete from ass_enterprise_docs where  file_id = #{fileId}
	</delete>

	<update id="cleanTaskEnterpriseDoc" parameterType="String">
		 update ass_enterprise_docs set task_id = '' where task_id = #{taskId}
	</update>



	<delete id="remove">
		delete from sys_file where id = #{value}
	</delete>
	
	<delete id="batchRemove">
		delete from sys_file where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<select id="listSpecialistDocFiles" resultType="com.bootdo.cpe.domain.SpecialistDocFileInfo">
		SELECT
		      ad.*, sf.url as file_url
		FROM
	      	ass_specialist_doc_files ad
	    	    JOIN
		    sys_file sf ON ad.file_id = sf.id
		<where>
			<if test="specialistUid != null"> and ad.specialist_uid = #{specialistUid} </if>
			<if test="groupName != null"> and ad.gruop_name = #{groupName} </if>
			and ad.del_flag = 0
			and ad.task_id = #{taskId}
		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
			<otherwise>
				order by sf.id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>

	<insert id="saveSpecialistDocFile" parameterType="com.bootdo.cpe.domain.SpecialistDocFileInfo" useGeneratedKeys = "true" keyProperty = "id">
         INSERT INTO `boot_do`.`ass_specialist_doc_files`
         (
            `task_id`,
            `file_id`,
            `specialist_uid`,
            `gruop_name`)
         VALUES
         (
		    #{taskId},
		    #{fileId},
		    #{specialistUid},
		    #{groupName}
		 )
	</insert>

	<delete id="deleteSpecialistDoc" >
		delete from ass_specialist_doc_files where file_id = #{id}
	</delete>

	<select id="fileTypeList" resultType="java.lang.String">
		SELECT
			file_type
		FROM
			ass_enterprise_docs
		where pro_id=#{proId} GROUP BY file_type
	</select>
</mapper>