<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bootdo.activiti.dao.QcGroupDao">

    <resultMap type="com.bootdo.activiti.domain.QcGroupDO" id="qcGroup">
        <result column="taskid" property="taskid"/>
        <result column="groupid" property="groupid"/>
        <result column="name" property="name"/>
    </resultMap>

    <select id="get" resultMap="qcGroup">
        SELECT * FROM ass_qc_group WHERE groupid = #{value}
    </select>

    <select id="list" resultMap="qcGroup">
        SELECT * FROM ass_qc_group
        <where>
            <if test="taskid != null">AND taskid = #{taskid}</if>
        </where>
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM ass_qc_group
        <where>
            <if test="taskid != null">AND taskid = #{taskid}</if>
        </where>
    </select>

    <select id="getGroupsByTaskId" resultMap="qcGroup">
        SELECT * FROM ass_qc_group WHERE taskid = #{taskId} ORDER BY groupid
    </select>

    <select id="isGroupNameExist" resultType="int">
        SELECT COUNT(*) FROM ass_qc_group WHERE taskid = #{taskId} AND name = #{groupName}
    </select>

    <select id="isGroupNameExistExcludeGroupId" resultMap="qcGroup">
        SELECT * FROM ass_qc_group
        WHERE taskid = #{taskId}
          AND name = #{groupName}
          AND groupid != #{groupId}
    </select>

    <insert id="save" parameterType="com.bootdo.activiti.domain.QcGroupDO">
        INSERT INTO ass_qc_group (taskid, groupid, name) VALUES (#{taskid}, #{groupid}, #{name})
    </insert>

    <update id="update" parameterType="com.bootdo.activiti.domain.QcGroupDO">
        UPDATE ass_qc_group SET name = #{name} WHERE taskid = #{taskid} AND groupid = #{groupid};

    </update>

    <update id="updatepro" parameterType="com.bootdo.activiti.domain.QcGroupDO">
        update ass_award_enterprise_project set qc_group_name =#{name} where publish_task_id = #{taskid} AND group_id = #{groupid};
    </update>

    <delete id="remove" parameterType="Integer">
        DELETE FROM ass_qc_group WHERE groupid = #{groupid}
    </delete>

    <delete id="batchRemove" parameterType="Integer[]">
        DELETE FROM ass_qc_group WHERE groupid IN
        <foreach item="groupid" collection="array" open="(" separator="," close=")">
            #{groupid}
        </foreach>
    </delete>
</mapper>
